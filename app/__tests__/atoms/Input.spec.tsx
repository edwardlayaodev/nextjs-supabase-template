/**
 * 1. Render the input with the correct type
2. Render the input with the correct label
3. Render the input with the correct placeholder
4. Apply the correct CSS classes based on `type`
5. Render icon at the start or end based on `iconPosition`
6. Apply error styling when there are validation errors
7. Call `onChangeHandler` and `onBlurHandler` correctly
8. Render error message when there are errors
 */
