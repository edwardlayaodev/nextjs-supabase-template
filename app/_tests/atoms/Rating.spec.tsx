/**
 * Here are some test cases for your Rating component:

    Render the correct number of star rating inputs based on maxRating
    Apply the correct name attribute to all rating inputs
    Call onChangeHandler correctly when a rating input changes
    Apply the correct CSS classes to the rating inputs and label
 */
