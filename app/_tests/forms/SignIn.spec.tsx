/**
Here are some test cases for your InnerForm component:

    Render the correct number of input fields
    Render input fields with correct labels and types
    Call handleChange, handleBlur, and handleSubmit correctly
    Update isOpen state based on props.status
    Display Molecule.Alert with the correct message and visibility
    Close the alert and reset props.status when handleClose is called
 */
